# Project Plans

This directory stores structured project plans created by the `project-planner` skill.

Each plan follows the format:
- `[feature-name].md` — Plan document with phases, tasks, dependencies, and agent assignments
